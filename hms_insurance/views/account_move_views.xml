<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_move_form_insurance_button" model="ir.ui.view">
        <field name="name">account.move.form.insurance.button</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="button_apply_insurance"
                        string="Apply Insurance"
                        type="object"
                        class="oe_highlight"
                        invisible="move_type != 'out_invoice' or state != 'draft'"/>
            </xpath>
        </field>
    </record>


</odoo>
